<nav class="bg-white border-gray-200 dark:bg-gray-900">
    <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4 text-white">
        <%= link_to root_path do %>
            <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">PaLevá</span>
        <% end %>

        <% if user_signed_in? %>
           <div class="flex items-center">
                <span class="mr-3"><%= current_user.name.capitalize %></span> <!-- Alinhar o texto do nome do usuário -->
                <%= button_to 'Sair', destroy_user_session_path, method: :delete, class: 'text-white' %> <!-- Estilização do botão -->
            </div>
        <% end %>
    </div>
</nav>